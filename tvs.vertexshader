#version 330 core

layout(location = 0) in vec3 vertexPosition;
uniform mat4 MVP;

out vec3 height;

void main()
{	
  gl_Position =  MVP * vec4(vec3(vertexPosition.x, vertexPosition.y, vertexPosition.z), 1.0);
  height = vertexPosition;
}

